import React from "react";
import Navbar from "../Navigation/Navbar/Navbar";

const Header = () => {
    return (
        <header>
            <Navbar />
        </header>
    );
};

export default Header;
